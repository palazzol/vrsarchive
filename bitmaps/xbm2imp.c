/*
 *	xbm2imp.c - convert X11 bitmap into IMPRESS bitmap
*/
#include <ctype.h>
#include <stdio.h>

/*
 *	Swap the bits in a byte left for right
*/
unsigned char revbyte[] = {
	,
};
#define putbyte(b)	putchar(revbyte[b])

/*
 *	Get a byte from the input file
*/
int
getbyte()
{	int val, c;

#ifdef SLOWWAY
	if (scanf(" 0x%x %*[,}]", &val) != 1)
		return(0x00);	/* Nice and white like the background */
#else
	while ((c = getchar()) != EOF)
		if (!isspace(c))
			break;
	if (c != '0')
		return(0x00);
	if (getchar() != 'x')
		return(0x00);
	if ((c = getchar()) == EOF)
		return(0x00);
	if (isdigit(c))
		val = (c & 0xF) << 4;	/* Assumes ASCII */
	else
		val = (9+(c&0xF)) << 4;	/* Assumes ASCII */
	if ((c = getchar()) == EOF)
		return(0x00);
	if (isdigit(c))
		val += (c & 0xF);	/* Assumes ASCII */
	else
		val += 9 + (c&0xF);	/* Assumes ASCII */
	while ((c = getchar()) != EOF)
		if (!isspace(c))
			break;
	/* Assume c is either ',' or '}' */
#endif
	return(val);
}

/* 320*8/300 = 8.53 inches at 300 dpi */
unsigned char swath[32][320];

main (argc, argv)
char **argv;
{
	int height, width, wremainder;
	int wpatches, hpatches, pwidth;
	register int x, y, y1, x1;
	char dummy[256];

	/* Get dimensions */
	scanf("#define %*s %d\n", &width);
	scanf("#define %*s %d\n", &height);
	while (scanf("#define %s %*d\n", dummy) != 0)
		fprintf(stderr, "%s: Ignoring %s record\n", argv[0], dummy);
	scanf("static char %*s = {\n");	/* } */
	width = (width+7)/8;	/* convert to bytes */
	width *= 8;				/* convert back to bits */
	wremainder = width & 31;
	pwidth = width & ~31;
	wpatches = (width+31) >> 5;
	hpatches = (height+31) >> 5;

	/* Put out the prologue */
	printf("@document(language impress, file \"%s\")", argv[1]);
	putc(213, stdout);	/* PAGE message- sets up parameters for Impress pg.*/
	putc(135, stdout);	/* SET-ABS-H: horiz. offset in points-req. 2 bytes */
	putc(00, stdout) ;	/*offset currently set to 250/300 of inch */
	putc(250, stdout);
	putc(137, stdout);	/* SET-ABS-V: horiz. offset in points-req. 2 bytes */
	putc(00, stdout) ;
	putc(250, stdout);	/* offset set to 250/300 of an inch. */
	putc(235, stdout);	/* Impress code for BITMAP */
	putc(07, stdout) ;	/* operation-type = 'OR' mapping of bits */
	putc(wpatches, stdout);	/* no. of patches horizontally */
	putc(hpatches, stdout);	/* no. of vertical lines of HPATCHES */

	/* Copy the image */
	for (y = 0; y < hpatches; y++) {
		/* Read in a swath */
		for (y1 = 0; y1 < 32; y1++) {
			for (x = 0; x < width/8; x++)
				swath[y1][x] = getbyte();
		}
		/* output the swath */
		for (x1 = 0; x1 < wpatches; x1++) {
			for (y1 = 0; y1 < 32; y1++) {
				putbyte(swath[y1][4*x1+0]);
				putbyte(swath[y1][4*x1+1]);
				putbyte(swath[y1][4*x1+2]);
				putbyte(swath[y1][4*x1+3]);
			}
		}
	}
	/* Emit the epilogue */
	putc(219, stdout);	/* ENDPAGE message for Impress  */
	putc(255, stdout);	/* EOF message for Impress  */
	return(0);
}
